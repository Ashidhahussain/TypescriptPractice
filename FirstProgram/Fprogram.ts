//To install Typescript used the command in cmd "npm install -g typescript", Now the package added
// then check the version with "tsc -v"
// To run typescript first use the "tsc filename.ts" , Now a new file will create with js
    // then run that js file with "node filename.js"

console.log("Hello World....")
